---
import BaseLayout from '../layouts/BaseLayout.astro'
import About from './About.jsx'
import { CLIENT } from '../config/client.js'
---
<BaseLayout
  title={`Nosotros | ${CLIENT.name}`}
  description={`Conoce al equipo de ${CLIENT.name}. ${CLIENT.description}`}
  canonical={`https://${CLIENT.domain}/nosotros`}
>
  <About client:load />
</BaseLayout>
